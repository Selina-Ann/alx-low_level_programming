#include "main.h"
/**
 * print_alphabet - print all the alphabet in lowercase
 */
void print_alphabet(void)
{
	char letter;
	for(letter = 'a'; ;etter <= 'z';letter++)
		_putchar(letter);
	_putchar('\n')
}
